# LLM provider (required — pick one)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Model to use (optional, default: gpt-4o-mini)
OPTOPSY_MODEL=gpt-4o-mini

# Data providers (optional — enables live data fetching)
EODHD_API_TOKEN=
